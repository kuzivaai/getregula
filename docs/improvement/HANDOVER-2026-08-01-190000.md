# Regula delivery-surface discovery handover — 2026-08-01

## Outcome

Repository-derived public-surface discovery is implemented at `777120f`.
`data/public_claim_surfaces.json` is generated from actual Pages, package,
README-link, Action, argparse and MCP delivery mechanisms; the narrow policy is
`data/public_surface_policy.json`; the readable reconciliation is
`docs/improvement/PUBLIC-SURFACE-DISCOVERY.md`.

The bounded implementation is working: 31 focused tests pass, inventory/report
freshness passes, and built wheel/sdist metadata matches its source. The repo as
a whole is not ready for general-user or commercial-pilot approval.

## Objective verdict

- `GIT_WRITE_AUTHORITY: ENABLED_WITH_APPROVED_ESCALATION`
- `ACTIVE_SURFACE_DISCOVERY: IMPLEMENTED`
- `BIDIRECTIONAL_RECONCILIATION: PASS`
- `OMISSION_MUTATIONS: PASS`
- `EXCLUSION_INTEGRITY: PASS`
- `EXACT_FINAL_VERIFICATION: RED`
- `SUBSTANTIVE_CLAIM_INTEGRITY: FAIL` (18 newly enumerated residual class/file
  pairs; wording deliberately unchanged)
- `PUBLIC_COPY_CHANGED: GENERATED_TEST_COUNT_ONLY`
- `MERGE_BLOCKER: RED` (309 findings over 85 files)
- `COMMERCIAL_V1_OVERALL_DECISION: STOP`
- `PRODUCT_PILOT_STATUS: NOT_APPROVED`
- `NEXT_DECISION: separately authorize residual active-copy remediation and
  resolve the preserved-log decomposition false positive, stale gap demo,
  environment doctor path, and pre-existing merge-blocker policy debt before
  considering merge or pilot approval.`

## Verification caveats

The clean custom runner was red solely on the previously preserved session log
timestamp false positive. Full pytest reproduced the same failure and was then
interrupted; it must not be represented as a complete green run. Doctor was red
on the unwritable home audit path, `build_gap_demo --check` was red, and the
merge blocker remained red. See the paired 190000 session log for exact evidence.

All pre-existing ledger items carried forward, including F25, F30, N35, N43,
N50/N54, N53, N55, N57, N6, N7, N10, N11, N12, N60 and N61. N62 records this
bounded discovery implementation without closing residual claims or readiness.
