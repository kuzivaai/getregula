# Regula handover — 2026-08-03

## Objective verdict

Regula is not yet objectively ready for an approved customer pilot or an
unqualified production-readiness claim. The active public-delivery inventory
and its omission controls are implemented, and the current inventory-wide
claim audit is clean. The delivery-aligned claim blocker reports zero
surviving findings, and the product has no completed commercial-v1 evidence
that would overturn STOP.

## What changed

- Claim enforcement now consumes the generated delivery-surface inventory.
- Inventory-wide auditing fails closed and scans active claim-capable text
  surfaces, including unchanged surfaces.
- Claim detection no longer accepts generic provenance words or a whole
  paragraph because one narrow exception matches.
- Public copy with unsupported fixed runtimes, stale generated counts,
  overbroad legal applicability or unsupported superlatives was narrowed or
  given primary/checked-in evidence. Detector behaviour, thresholds, package
  version and commercial-v1 inputs/results/gates were not changed.
- English changes that affected localized landing content were synchronized
  to DE and PT-BR.

## Evidence and remaining work

The active scan reports 96 files, 532 claim candidates and zero unsourced
candidates. Fact verification reports 148 matching references across 17
files. Site integrity reports no dead internal references. Mutation coverage
exercises new delivery candidates, stale policy, duplicate IDs, missing
dispositions, ignored/untracked inputs, Git/build/parser failures and
legitimate negatives.

The previous 42-item result used tracked-renderable Markdown as a publication
proxy. The delivery-derived gate reports zero active-product residuals while
preserving those historical records. Full pytest reported 2,673 passes and
eight sandbox socket setup errors; the affected timestamp module then passed
23/23 with loopback permission. Complete detached verification remains due.

No broad documentation deletion was performed. Historical and internal audit
records are retained and classified rather than erased. No new blog was added:
the available skill catalogue did not include `/humaniser`, and publishing
more content would not resolve the evidence or user-journey blockers.

## Decisions

- `GIT_WRITE_AUTHORITY: ENABLED_IN_AUTHORISED_CONTEXT`
- `ACTIVE_SURFACE_DISCOVERY: IMPLEMENTED`
- `BIDIRECTIONAL_RECONCILIATION: PASS_FOR_GENERATED_INVENTORY`
- `OMISSION_MUTATIONS: PASS`
- `EXCLUSION_INTEGRITY: PASS`
- `EXACT_FINAL_VERIFICATION: PENDING`
- `SUBSTANTIVE_CLAIM_INTEGRITY: IMPROVED_NOT_COMPLETE`
- `PUBLIC_COPY_CHANGED: YES_NARROWING_AND_SOURCING_ONLY`
- `ACTIVE_CLAIM_MERGE_BLOCKER: GREEN`
- `OVERALL_MERGE_READINESS: PENDING_CI_AND_DETACHED_VERIFICATION`
- `COMMERCIAL_V1_OVERALL_DECISION: STOP`
- `PRODUCT_PILOT_STATUS: NOT_APPROVED`
- `NEXT_DECISION: push the improvement branch, require CI, then merge through the protected workflow if checks pass`
