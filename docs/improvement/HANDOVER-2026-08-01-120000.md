# Regula repository-first continuation handover — 2026-08-01 120000

## Outcome

This session stopped before public-copy or release edits. The next binding repository constraint is incomplete active public-surface discovery. The existing contract has 22 paths, while a Git-derived candidate set has 162 tracked reader-facing/documentation/CLI/site paths. The current contract's bidirectional test is hard-coded and therefore cannot detect a newly public surface.

## Authority and environment

`/status` was unavailable (`/bin/bash: /status: No such file or directory`). Workspace: `/home/USER/getregula`; sandbox `workspace-write`; repository and `/tmp` writable; network restricted by default; approved escalation available; `.git` readable and worktree operations available; no concurrent writer observed; subagent delegation available. Applicable instructions are `AGENTS.md` and `.claude/rules/{git,measurement,tests,quality-standards,python-scripts,site-html,regulatory-content}.md`.

## Current state

```text
HEAD 56044d1b8c26419149803e41cd6c0826f097d7df
tree bbad19b080e9af18b2edaffb3837ca7f7b0c6eb9
main/origin/main b5ac95c87e3f8fbc1a44551ce2815c3744c9050d
origin/improvement/2026-08-programme e48c4dbe8fbc1f6550393a6fe4dc229f6e72c9e3
ahead of origin/main: 121 commits
working tree: evidence-only session log and this handover before commit
```

The latest implementation commits are `e6fffde` (release identity erratum) and `56044d1` (claim corrections). The commercial STOP decision and `PRODUCT_PILOT_STATUS: NOT_APPROVED` remain unchanged.

## Candidate reproduction and ranking

Candidate A is reproduced: `data/public_claim_surfaces.json` contains 22 active paths, while the Git-derived candidate set contains 162 tracked paths. Omitted examples include `action.yml`, the German and Portuguese assessment pages, most site guides/blogs/regions, `site/sitemap.xml`, and additional CLI modules. The current test compares against a hard-coded 22-path set, so it passes while omitting those surfaces.

Candidate B remains a release-owner issue: public PyPI 1.9.0 and local 1.9.0 have different bytes; the prior dated erratum records this. Candidate C is not a product-tree defect: the complete suite previously passed on the exact implementation commit, but the evidence-only commit requires its own clean verification. Candidate D remains a pre-existing red merge blocker with unsourced-claim findings. Candidate E remains the commercial constraint because no independent labels, buyer interviews, design-partner access, or paid validation exist.

Ranking: A (highest consequence and finite repository closure), E (external commercial evidence), D (policy/sourcing decision), B (owner-controlled release), C (verification after evidence commit).

## Selected unit and stop boundary

The next unit is a repository-derived active-surface inventory and omission detector. It should derive delivery surfaces from GitHub Pages inputs, package metadata, Action/MCP/CLI registrations, sitemap/navigation and machine-readable outputs; classify historical records explicitly; and fail on omitted/new surfaces. Required controls: bidirectional discovery, explicit narrow exclusions, omitted-surface mutation, newly-added-surface mutation, and a clean final verification.

This session did not implement it because correcting the discovered active public claims and release artifacts is owner-gated by the directive. No public positioning, version, detector, threshold, classifier, benchmark input, label, result, deployment, publication, tag, upload, merge, or push was changed.

## Verification evidence retained

The permanent command-by-command evidence record is [SESSION-LOG-2026-08-01-120000.md](SESSION-LOG-2026-08-01-120000.md). The prior implementation verification recorded `2646 passed`, custom runner `1386 passed`, self-test `6/6`, and six fast gates passing; the merge blocker remained red. This session's evidence-only commit must be verified separately before describing the evidence record as final.

## Owner recommendation

Authorize only the repository-derived inventory/control unit next. After it produces the exact active surface set, authorize a separate public-copy and release correction unit. Strongest counterargument: the 162 tracked candidates may include internal or non-reader-facing material; the delivery-graph classifier must establish publicness before treating every omission as a claim defect.

Default: STOP.
