# Session Handover
Generated: 2026-04-04T22:17:01+01:00
Project: getregula

## Completed This Session

### Audit & fixes
- Deep go-live audit via research-eval — found stale PyPI package (critical), missing `--version` flag, hardcoded version strings in report.py / pdf_export.py / mcp_server.py
- Fixed all hardcoded version strings (were showing v1.1.0/v1.2.0, now pull from constants.VERSION)
- Added `regula --version` flag (was missing entirely)
- Bumped to v1.5.1 (PyPI v1.5.0 was built before `regula assess` was added — first README quickstart step was broken on clean install)

### Distribution
- Rebuilt and published `regula-ai==1.5.1` to PyPI — verified `regula assess` works on clean install
- Cold install confirmed: `pip install regula-ai==1.5.1 && regula --version && regula assess` all pass

### Domain
- Created CNAME file (`getregula.com`) committed to repo
- DNS records configured at registrar: 4x A records (GitHub Pages IPs) + CNAME www → kuzivaai.github.io
- Deleted conflicting GoDaddy WebsiteBuilder A record and circular www CNAME
- Deleted Vercel integration (was trying to deploy as Python web app, not needed)
- DNS check passed on GitHub Pages — awaiting TLS cert provisioning (in progress)

### Commits
```
658c3ab release: v1.5.1 — fix stale PyPI package, add --version flag, connect domain
```

## In Progress
- TLS cert provisioning for getregula.com — GitHub Pages is issuing it, usually completes within 15 min of DNS check passing. Once done: tick "Enforce HTTPS" in Settings → Pages.

## Not Yet Done
- HN post not drafted
- GitHub release tag v1.5.1 not created
- CONTRIBUTING.md not added
- Email capture on landing page not set up
- HN account not created/aged

## Known State
- 532 tests pass, self-test 6/6, doctor 8 pass
- PyPI: regula-ai 1.5.1 is the only published version
- Landing page deploying via GitHub Pages on push to main
- getregula.com DNS propagated, HTTPS pending

## Resume Prompt
> Read .handover.md and continue where the last session left off.
