# Session Handover — 2026-04-06

## Completed This Session

### Bug Fixes (8)
1. `cmd_fix` passed dict to `get_remediation` — all fix scaffolds returned wrong guidance → FIXED
2. `classify -i` on text descriptions returned NOT_AI — domain keywords gate blocked Annex III → FIXED (50+ domain keywords)
3. SARIF undefined rules for agent_autonomy/ai_security → FIXED
4. `\bface.?recogn` didn't match "facial recognition" → FIXED
5. `\bresume.?filt` didn't match "resume screening" → FIXED
6. `discover` didn't respect `# regula-ignore` → FIXED
7. Hardcoded version in test → FIXED (semver regex)
8. Missing import in report.py → FIXED

### Security Fixes (3)
1. `.yaml`/`.json` removed from doc bypass — secrets always checked
2. MCP server path validation — blocks root/system dir scans
3. `--diff` git ref validation — regex guard

### New Features (2 moat features)
1. `regula conform .` — Article 43 conformity assessment (25 files, 12 folders)
2. `regula sbom --ai-bom .` — CycloneDX 1.6 AI-BOM with GPAI tiers (38 models)

### Omnibus-Aware Deadline Tagging
- Every finding now has deadline/omnibus_deadline fields

### Timeline Updated to April 2026

### Numbers: 330 patterns, 37 commands, consistent across all pages

## Test Results
- 797/797 passed (365 functions), self-test 6/6, doctor 8/8

## Not Yet Done
- Cross-file data flow analysis (Moat 3) — research in progress
- Commit not yet made — user hasn't asked
