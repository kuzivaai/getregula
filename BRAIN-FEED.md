# Brain feed

The five lines below are read mechanically by `~/kuziva-brain`
(`scripts/sync-projects.sh`, daily). Keep them current before ending a session in
which anything material changed. Contract and rationale:
`~/kuziva-brain/state/projects/FEED-CONTRACT.md`.

Write prose, not metrics. Any number here must carry its source in the same
line, because this file is a tracked markdown surface and the claim auditor
scans it like any other.

<!-- BRAIN:BEGIN -->
state: Development-time AI governance risk-indication CLI, published and deployed. The claim gates are green on main and the commercial gates are not.
blocker: The controlling commercial verdicts (STOP, PRODUCT_PILOT_STATUS NOT_APPROVED, TECHNICAL_EVIDENCE FAILED) stand until new evidence clears each gate on its own terms; no engineering increment moves them.
next: Owner decisions before engineering, per the priority action plan in the 13 August end-to-end dossier: name the seller and consultant, define the service scope, take qualified advice on terms, refunds, privacy and tax. Separately, review three committed workstreams on feat/engagement-fixes (PR #55 is still open, nothing is on main): the browser-assessment rework, where the questionnaire flow is now one shared module instead of three inline copies; a claim-integrity pass that verifies quoted regulatory passages against cached primary corpora and rebuilds the prohibited-claim guard to read accented and marked-up copy in all three shipped languages; and a documentation pass that removed published CLI transcripts asserting a legal classification and an obligation the tool does not make, with a gate that re-runs the command each page documents. The detector priority that did not reproduce across checkouts of the same commit is now diagnosed and fixed: LEDGER N108 raised it, N112 traced it to the scan cache, and N113 records that it was worse than N112 concluded. The cache key carried the path relative to the scan root while provenance came from the full path, so a cached entry written by an examples copy was served to a production file of the same relative name, and because `--scope production` filters on provenance the production file's finding disappeared from the scan entirely. That is a false negative, not a priority wobble. The key now carries a path-context component, guarded by tests each shown to fail under a control that removes it. The change was made against N112's recorded decision to defer it under PRODUCT_BUILD STOP, on the grounds that the severity was established to be different; it is unpushed and reversible and the owner should rule on it. A fourth commit on that branch is LEDGER N109: the weekly PyPI metrics artefact had been recording a whole-period cumulative total under a weekly label since April, because the label was a constant in the collector rather than something derived from the data, and nothing under the metrics directory had any test. The collector now derives its own window and records the boundaries, the historical rows are relabelled with their values left intact, and a new guard exercises the checks against constructed rows so they cannot pass by matching nothing. Treat any download figure quoted from before 14 August 2026 as withdrawn; re-fetch it, date it, and say whether mirrors are included. An independent audit on 15 August re-ran every gate the previous handover cited and reproduced all of them, but found that its "open items" count could not be reproduced at all, because the ledger recorded state only as prose; every entry now carries a machine-readable field and `scripts/ledger_status.py` derives the counts, which differ by more than twenty depending on whether "open" means substantive work outstanding or anything outstanding at all. The same audit widened the lint gate, which had been reported as lint cleanliness while selecting a narrower rule set than the debt measured against it. A remediation pass on 15 August then actioned the items that audit left open, recorded as LEDGER N117 to N124, all unpushed. Three are worth the owner's attention before the rest. The accessibility gate had never been run on this branch and was failing: two pages had a horizontally scrolling code block a keyboard user cannot reach, which is a Level A criterion on a site targeting AA, and the cause was the transcript correction three commits earlier lengthening the lines inside those blocks; the job only triggers on pull requests, so it is blind to work that stays local, and the branch has never been pushed. The published test-function count was measuring module-level functions only against a label reading "all files", so every test written as a unittest method was invisible, and the regex behind it also counted a function that exists only inside a quoted code sample; it is now parsed with the abstract syntax tree and cross-checked per file against what pytest actually collects. And `regula plan --done` accepted any identifier at all, writing a completion record into the evidence directory for a plan the command never emits, which it now refuses to do. The audit's own description of the claim quarantine was also wrong in a useful direction: six of the entries it called live had been fixed long ago and were holding ratchet headroom for findings that no longer existed, so the backlog and its ceiling both fall by nine. A dossier pass at the end of the same day then found the item that now sits above everything else, recorded as N125 and deliberately left unfixed because it is a product change under the standing stop: three shipped paths state a compliance determination, which is the one thing this project's own rules forbid outright. The badge command emits a pasteable markdown badge labelled with the regulation and carrying a green compliance verdict, for a directory whose only content prints a greeting, and a badge is built to sit in someone else's README where it travels with no disclaimer. The code-generation command asserts Article 50/52 transparency compliance from two documentation files existing on disk, without reading either, and wires that to a continuous-integration exit code. The third describes the disclosure text the tool drafts as already satisfying the law, on a public page. Separately, a regulatory date needs an owner with the primary sources: four records give three different answers for when the South African draft policy was withdrawn, two of them contradicting themselves inside a single card and inside the sourced generator file; that is LEDGER N127 and it is still open. On 17 August the compliance-determination class was closed, recorded as LEDGER N129, and three things about it are worth the owner's attention. It was eleven sites across eight files rather than the three N125 named, and the site that actually turns the determination into a continuous-integration exit code was not among them, so a fix guided by that entry alone would have corrected the advertisement and left the behaviour. The guard N125 blamed was not the guard at fault: the multilingual claim guard built in the previous session has arms for four claim classes and none for asserting that a compliance state holds, so nobody had written the arm. And the badge was re-expressed rather than removed, which departs from the instruction given, because a surviving honest form exists once the label stops naming the regulation and the message reports what the scan found; its caveat is now the badge's own link target, since the objection to a badge is that it detaches from every qualification on the page that made it. That is a product change under the standing stop and the owner should rule on it; it is unpushed and reverts in one commit. Also on 17 August: the whole of the 15 August session turned out to be uncommitted rather than committed-and-held as its own ledger entries said, and is now committed after being verified as a whole. Two new open items. The first visual on the project README is a terminal recording in a vector file, and three separate instruments skip that file type, so the front page publishes output from an older release with a framing the project retired everywhere it could see; that is LEDGER N130 and it replaces the imagery work the brief asked for, because the site already carries real product output and the problem is that the output it carries is stale. The second is that the published command count is a fourth quantity the cascade tool does not propagate, so it drifts by hand across three languages (LEDGER N131). Finally, the three pricing-transparency statistics supplied for a pricing decision all fail at source, one of them attributed to a firm that had ceased operating before the year it is dated to (LEDGER N132), and Google Search Console is unreachable by both available routes, so every search figure quoted from it this month is inherited and unverified. The South African withdrawal-date disagreement is now researched and resolved, recorded as LEDGER N134, and the finding is not the one the open item expected: the four records did not disagree about one date, they were calling three events by one name, and the act that actually withdrew the draft, a gazette of 12 June 2026, was recorded on no surface here. Every surface dated the withdrawal by when it was announced or approved instead, which for a tool about regulatory obligation is the wrong kind of date to publish. Two things about that entry are worth more than the correction. A false correction was very nearly published to a file that was right, and the only reason it was not is the rule requiring both artefacts to be read before a contradiction is asserted. And the earlier hypothesis that the project instruction file's date was a conflation with a Colorado enforcement stay is refuted; it was a one-day error about a real event. The gazette itself could not be opened because gov.za refused connection, so that row is marked secondary in the tracker's own vocabulary and no gazette number is asserted anywhere. Later on 17 August the front-page recording item was closed and turned out to be wider than recorded, as LEDGER N135 to N142. The stale terminal recording was one of five carriers publishing a framing the project retired: an architecture document listed it as a design principle of the shipped product, and two standards crosswalk files described the tool's output by the retired name. Four surfaces published a risk-tier line stripped of the sentence the tool prints beside it, which no retired-output list could catch because the line is current and true; that needed a second mechanism which runs the command and requires the tool to emit the qualifier too. One of those four was a demonstration transcript whose own README described it as hand-authored to match real output, and whose figures no invocation produces, and it had been copied onto a published regional page. The three recordings are removed and the README now carries a real transcript bound to a command that is re-run on every check; that departs from the instruction to regenerate them, because a regenerated recording publishes the recorder's home directory path, cannot be checked for currency, is unreadable in review and gives a screen reader nothing, and it reverts in one commit if the owner disagrees. A separate pre-existing hole was found while deciding which instruments should read that file type: the delivery-surface audit reported a clean result over six surfaces it cannot read and said nothing about them, so it now declares who covers each and what nobody covers. The command count is now cascaded from the argparse registry in three languages, cross-checked against the handler scan so a command registered with no handler refuses instead of guessing. The fixture directory whose name asserted compliance is renamed. And the research the previous brief asked for is written to docs/venture/research-2026-08/: the load-bearing result is that at 188 visitors over 91 days, measured from the Plausible export of 14 August, no before-and-after claim about this site is establishable, because a full year of split testing could not detect a change smaller than about 1.7-fold. Design work here is a precondition, not an intervention, and must never be credited with a measured lift.
gates: Claim freeze holds for external use. No precision figures, no test count, no pattern count, no version number, no cross-map count anywhere outside this repo. Never present a scan as a compliance determination, or "not flagged" as "compliant". Payment and booking stay disabled until every P0 control is complete.
updated: 2026-08-17
<!-- BRAIN:END -->

## Context the block cannot carry

The claim quarantine (`.claim-quarantine.json`) is the thing most easily
misread from outside. The published-surface gate passing does not mean the
published claims are reconciled: the quarantine holds pre-existing claims that
its own status field records as unverified and not endorsed, so that the gate
can pass for new claims while that backlog burns down. Anyone quoting this
project's numbers should read the quarantine before deciding a green gate means
what they want it to mean.
