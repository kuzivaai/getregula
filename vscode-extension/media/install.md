# Install the Regula CLI

Regula VS Code runs the Regula CLI under the hood. Install it with:

```bash
pipx install regula-ai
```

Or with pip:

```bash
pip install regula-ai
```

Verify the installation:

```bash
regula --version
```
