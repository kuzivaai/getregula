$ pip install regula-ai
Successfully installed regula-ai-1.9.0

$ cat demo.py
import openai
def classify_resume(resume_text):
    client = openai.OpenAI()
    reply = client.chat.completions.create(
        model='gpt-4',
        messages=[{'role':'user','content':f'Score this resume: {resume_text}'}])
    return reply.choices[0].message.content

$ regula quickstart

Regula Quickstart

  Created: ./regula-policy.yaml

  First scan complete (0.0s)
  Files scanned:      1
  BLOCK findings:     0
  WARN findings:      0
  INFO findings:      1

  Next steps:
    1. Edit regula-policy.yaml — set your organisation name and AI officer
    2. Run 'regula check .' for the full detailed scan (every finding, not just the top 3)
    3. Run 'regula doctor' to verify your setup
    4. Run 'regula install claude-code' to add pre-commit hooks
    5. Run 'regula gap --project .' for compliance gap analysis
    6. Run 'regula bias' to evaluate model stereotype bias (requires Ollama)

$ regula classify --file demo.py
Decision: insufficient_information
Model: 2026-08-12.4
Jurisdiction: eu
Rule resolution: unresolved
Facts needed to resolve the next decision: 2
  - is_ai_system: Does the subject meet the governing law's definition of an AI system or regulated automated technology?
  - jurisdiction_in_scope: Does this jurisdiction's territorial and operator scope apply?

Detector observation (not a legal classification):
HIGH-RISK: Employment and workers management - Articles 9, 10, 11, 12, 13, 14, 15

# The detector matched Annex III Category 4 patterns. Whether the Act applies,
# and whether those articles are owed, depends on facts the scan cannot see and
# that the decision block above names. Regula reports the indicators; it does
# not determine the tier.
